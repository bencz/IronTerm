CREATE TABLE BENCZ1.NAMESP (
    NASEQ# DECIMAL(5) NOT NULL
        GENERATED ALWAYS AS IDENTITY (START WITH 1),
    NANAME CHAR(30) NOT NULL,
    NAADDRESS1 CHAR(30) NOT NULL,
    NAADDRESS2 CHAR(30) NOT NULL,
    NACITY CHAR(30) NOT NULL,
    NASTATE CHAR(2),
    NAZIP CHAR(10),
    PRIMARY KEY (NASEQ#)
) RCDFMT NAMESPR;

INSERT INTO BENCZ1.NAMESP
    (NANAME, NAADDRESS1, NAADDRESS2, NACITY, NASTATE, NAZIP)
VALUES
    ('IBM ROCHESTER', '2800 37TH ST NW', '', 'ROCHESTER', 'MN', '55901');
